[project]
name = "contextscore"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.6",
    "pre-commit>=4.6.0",
    "pytest>=9.0.3",
    "sentence-transformers>=5.5.1",
    "torch>=2.12.0",
]
